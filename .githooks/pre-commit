#!/usr/bin/env bash
set -euo pipefail

npm run lint

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
else
  echo "gitleaks is not installed; install it before committing sensitive changes." >&2
  exit 1
fi
